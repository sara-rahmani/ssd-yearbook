1. Edit 
   - Manager can edit roles
   - User can edit their email, fname, lname, interests,profile picture, comments.-----> Done

2. delete profile -----> Done

4. Search - The user listing page should allow searching/filtering by any of the name fields, email
  address, or interests -----> Done
  

3.Session - session timeout of 20 mins.


5. Comments - users can add comments on other profiles.  

6. Styling 


 <% if (roles.includes("Admin") || roles.includes("User") || reqInfo.username == profiles[i].username ) { %>

  <a href="/user/register/<%= profileId %>"><button>Edit</button></a>
  <%}%>
<input name="username" type="checkbox"
                               value="<%= user.roles ? user.roles : '' %>">